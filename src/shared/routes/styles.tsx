import { StyleSheet } from "react-native"

export const TabBarStyles = StyleSheet.create({
    TabBarStyle: {
        height: '10vh',
		paddingBottom: '10px',
    },

    TabBarIconStyle: {
        marginBottom: '-1vh',
    },

    TabBarLabelStyle: {
        marginTop: '-0.5vh',
		fontSize: 17,
		fontWeight: '600',
    }
})

import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  button: {
    position: 'absolute',
    bottom: 0,
    alignItems: 'center',
    padding: 9,
    width: '80vw',
    borderRadius: 30,
    borderColor: '#00B9C6',
    borderWidth: 3.5,
  },
});

export default styles;
